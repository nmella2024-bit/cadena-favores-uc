rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    // Reglas para la colección de favores
    match /favores/{favorId} {
      allow read: if true;
      allow create: if request.auth != null && request.resource.data.usuarioId == request.auth.uid;
      allow update: if request.auth != null && request.auth.uid == resource.data.usuarioId;
      allow delete: if request.auth != null && request.auth.uid == resource.data.usuarioId;
    }

    // Reglas para la colección de usuarios - TEMPORALMENTE ABIERTAS PARA DEBUGGING
    match /usuarios/{userId} {
      allow read: if true;
      allow write: if true;
    }
  }
}
